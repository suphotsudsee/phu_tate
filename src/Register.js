import React from "react";

// Placeholder register page (ยังไม่ใช้)
const Register = () => (
  <div style={{ padding: 20, textAlign: "center" }}>
    <h2>Register</h2>
    <p>หน้านี้ยังไม่ได้เปิดใช้งาน</p>
  </div>
);

export default Register;
